# Worker Inbox

The parent/governor may copy verified worker summaries here after checking worker-owned outputs.

Workers must write first to their own external run directories, not directly to this target repository.

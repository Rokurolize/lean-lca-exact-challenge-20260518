# W1298 provider search ledger

W1298 records the canonical W987 provider boundary after the W1297 candidate consolidation.

The only direct unpacking of `BoundaryRelationTargetClosedEmbeddingClosednessNormalizedSourceEndpointLocalizedUnitFullDataProviderW987` exposes the W730 boundary/relation provider and the W986 target/closedness/normalized-source/endpoint-localized provider. This is the existing W986 two-input surface, so `canonicalFullDataUnpackExpandsCurrentSurfaceW1298` proves it is larger than the W1270 one-input W987 surface.

The theorem `repackFullDataOfFullDataW1298_eq` proves that repacking the two canonical projections is exactly the original W987 provider. This rules out the canonical unpacking as a closing move; it only expands the remaining provider obligation.

W1298 is nonterminal and keeps `productSuccessClaimed := false`.

# W928 Provider Search Ledger

W928 extends the W927 closed-map target-compact-nonbottom-primitive-field route by replacing the outer target-codomain compactness filter typeclass assumption `[Filter.NeBot f]` with the explicit raw non-bottom assumption `f ≠ ⊥`.

The route leaves the principal-universe support assumption and the inner neighborhood-filter meet non-bottom conclusion unchanged. It reconstructs W927's outer `Filter.NeBot f` field from the explicit inequality before delegating to the W927 stable route.

This is still nonterminal for the original four-task contract: W928 preserves the stable W927 ExactAt route output, keeps `productSuccessClaimed = false`, and records 39 explicit input names with the target-codomain compactness input refined from an outer typeclass assumption to the underlying non-bottom inequality.

# W1343 Provider Search Ledger

## Target

W1343 inspects the W512 relation-fields closed-map bridge:

`WppOpClosedNatTransOrdinaryRelationFieldsV370SupportW512`

Guard file:

`ClosedMapBoundaryRelationTargetSurjectiveCompactClosedEmbeddingClosednessNormalizedSourceEndpointLocalizedUnitFullDataRelationFieldsClosedMapFieldSplitGuardW1343.lean`

## Findings

W512 replaces W511's descended closed-map input with two relation fields: relation-pullback for the ordinary descended quotient map and target-relation lifts through the ordinary codomain component. Closedness of the natural-transformation component supplies the component closed-map input needed by the generic W512 closed-map bridge.

The guard proves that W512 preserves the relation-pullback field, routes its provider through W511, completes W510's topology-facts provider through W511, and delegates selected-colimit, mapped-explicit-cokernel, and closure-kernel exact-acyclic endpoints through W511. It also records the W513 consequence that the target-relation lift field becomes a surjectivity-level requirement when the target relation is top.

## Result

This is useful route evidence, but it does not close the original four-task contract. The W512 local boundary still exposes a two-field relation package plus a preservation provider, and remains larger than the current one-input W987 obligation. Product success remains false.

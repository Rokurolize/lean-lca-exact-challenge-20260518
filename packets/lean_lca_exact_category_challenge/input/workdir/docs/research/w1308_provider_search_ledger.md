# W1308 Provider Search Ledger

W1308 inspects `EndpointKernelCokernelTopDataProviderW974`, the endpoint kernel/cokernel-top provider exposed near the W973 closed-embedding closedness route.

The canonical W974 unpacking exposes four endpoint witnesses: closed embeddings, open maps, cokernel-subgroup-top witnesses, and kernel equalities. The checked W974 route surface has thirteen inputs. Both surfaces are larger than the current W1270 payload surface, which is the single W987 full-data provider input.

Lean declaration `repackEndpointKernelCokernelTopDataW1308_eq` records that projecting and repacking W974 returns the same provider. The W974 boundary therefore documents another non-closing provider boundary, not a completion witness.

Product success is not claimed. The original four-task contract still lacks a product-success terminal outcome and review packet.

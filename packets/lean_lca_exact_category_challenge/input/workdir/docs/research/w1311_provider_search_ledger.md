# W1311 Provider Search Ledger

W1311 inspects `UnitMappingConeStrictExactIsoDataProviderW981`, the unit mapping-cone strict-exact/isomorphism provider that constructs the W976 unit kernel/cokernel-top provider.

The W981 unit boundary exposes three inputs: the unit mapping-cone model, a strict short-exact family, and the chosen isomorphism. The checked W981 full-data route surface has seven inputs. Both surfaces are larger than the current W1270 payload surface, which is the single W987 full-data provider input.

Lean declaration `repackUnitStrictExactIsoDataW1311_eq` records that repacking W981 unit data returns the same provider. The W981 boundary therefore documents another non-closing route, not a completion witness.

Product success is not claimed. The original four-task contract still lacks a product-success terminal outcome and review packet.

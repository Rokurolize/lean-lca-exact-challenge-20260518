# W919 Provider Search Ledger

W918 is the current closed-map triangle-completion-raw-field boundary-raw-fields route. It verifies a nonterminal stable ExactAt route with 39 explicit inputs, including source-category `Pretriangulated` and `IsTriangulated` fields for `BoundedComplexCategory MetrizableLCA`.

The next supported boundary is the source triangulation pair. The current Lean environment does not infer `Pretriangulated (BoundedComplexCategory MetrizableLCA)` or `IsTriangulated (BoundedComplexCategory MetrizableLCA)` without explicit evidence, so these fields must stay visible as real source-category obligations rather than being hidden behind a broader provider.

W919 replaces the W918 source pretriangulated and triangulated field shapes with raw source-category propositions. It reconstructs the W918 assumptions through `sourcePretriangulatedOfRawFieldW919` and `sourceTriangulatedOfRawFieldW919`, then projects back to W918 through `w918LeavesOfSourceRawFieldsW919`.

This is intentionally nonterminal. The W919 source triangulation propositions remain assumptions in the closed-map branch route, and no original-four-task completion witness or final review packet is claimed.

After W919, the route still has 39 remaining explicit inputs. Deferred provider boundaries include source-image normalization, strict-preimage coherence consolidation, branch-selection or no-argument witnesses, endpoint facts, localized-right-adjoint data, and the final review packet.

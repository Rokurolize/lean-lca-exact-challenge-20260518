# W981 provider search ledger

Baseline: W980 constructed the W974 endpoint kernel/cokernel-top provider from one endpoint strict short-exact family, then delegated through the W979/W978/W977 route stack. Product success remained false because seven provider-level inputs were still explicit, including the W976 unit mapping-cone kernel/cokernel-top isomorphism data provider.

Decision: W981 targets the W976 unit mapping-cone data surface. It keeps the unit mapping-cone model and chosen mapping-cone isomorphism, but replaces the four separate degreewise closed-embedding, open-map, cokernel-top, and kernel-equality fields with one unit mapping-cone strict short-exact family. W981 then reconstructs the existing W976 provider by projecting closed inclusion and open map from strict exactness, deriving cokernel-top from surjectivity, and deriving kernel equality from algebraic exactness.

Rejected alternatives:

| Candidate | Effect | Reason deferred |
| --- | --- | --- |
| Construct the W976 unit mapping-cone provider outright | Remove the unit mapping-cone provider boundary | Still unsupported: no no-argument construction exists for the chosen unit mapping-cone model or chosen isomorphism. |
| Rework localized adjunction first | Potentially remove the right-adjoint/adjunction provider | No local no-argument localized adjunction constructor was identified in the current surface. |
| Merge endpoint and unit strict exactness into one larger provider | Reduce visible field count | This would be broader packaging rather than a local mathematical reduction. |

Result: selected for W981. The route verifies that W976 unit kernel/cokernel-top fields are constructible from unit mapping-cone strict short exactness. Product success remains false because W981 still assumes seven provider-level inputs and no original four-task completion witness exists.

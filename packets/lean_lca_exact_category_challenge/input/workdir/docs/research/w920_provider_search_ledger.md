# W920 Provider Search Ledger

Baseline: W919 is the current closed-map source-raw-fields route. It verifies a nonterminal stable ExactAt route with 39 explicit inputs, including the W511 `ClosedNatTransOrdinaryRelationPullbackConditionW511` and W512 `ClosedNatTransOrdinaryTargetRelationLiftsConditionW512` aliases.

Search result: those W511/W512 names are abbrevs. The W511 relation-pullback condition unfolds to the primitive `AddSubgroup.comap` equality for the target cokernel subgroup along the ordinary codomain component. The W512 target-relation-lift condition unfolds to the primitive quantifier saying every element of the target cokernel subgroup lifts through that codomain component.

Chosen increment: W920 exposes those two primitive propositions directly through `relationPullbackPrimitiveW920` and `targetRelationLiftsPrimitiveW920`, then reconstructs the W919 fields with `relationPullbackConditionOfPrimitiveW920` and `targetRelationLiftsConditionOfPrimitiveW920`.

This is intentionally nonterminal. W920 still assumes the primitive relation equality and target-lift quantifier, and no original-four-task completion witness or final review packet is claimed.

After W920, the route still has 39 remaining explicit inputs. Deferred provider boundaries include source-image normalization, strict-preimage coherence consolidation, branch-selection or no-argument witnesses, endpoint facts, localized-right-adjoint data, source-category triangulation evidence, and the final review packet.

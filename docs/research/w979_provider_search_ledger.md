# W979 provider search ledger

Baseline: W978 constructs the W977 full-data provider from the W971 component-field surface. It verifies that the full-data provider is constructible from 18 explicit W971 component fields, but product success remains false because those fields are still assumptions.

Decision: W979 adds the reverse bridge: split one W977 full-data provider back into the W971 component-field record, then rebuild the W977 provider through W978. This gives the current full-data provider an explicit component-field extraction surface without asserting that any component field has been constructed without assumptions.

Rejected alternatives:

| Candidate | Effect | Reason deferred |
| --- | --- | --- |
| Claim W978 component fields are solved | Treat the split fields as no-argument evidence | Unsupported: W979 only projects fields from a W977 provider and does not construct that provider. |
| Attack one W971 topological field directly | Potentially remove a real assumption | More valuable, but the current local pass did not identify a no-argument constructor for boundary/relation, target no-univ, closed-embedding closedness, endpoint, localized-adjunction, normalized-source, or unit mapping-cone fields. |
| Stop at W978 one-way construction | Leave W977 to W971 extraction implicit | W979 makes the bridge bidirectional and gives future work a named splitting surface for the 18 W971 obligations. |

Result: selected for W979. Explicit input names are 1 because the route starts from the W977 full-data provider; the underlying split surface still exposes 18 W971 component obligations. Product success remains false because no no-argument full-data provider or original four-task completion witness exists.

# W918 Provider Search Ledger

W917 is the current closed-map boundary-raw-fields relation-raw-fields route. It verifies a nonterminal stable ExactAt route with 39 explicit inputs, including the source-side `MetrizableBoundedExactWeakEquivalenceTriangleCompletionInputW654` field.

The next supported boundary is the W654 triangle-completion input. This field is an abbrev for the raw proposition that compatible maps between distinguished triangles, whose first two vertical maps are bounded exact weak equivalences, admit a third bounded exact weak equivalence completing the morphism of triangles.

W918 replaces the W917 `triangleCompletion` field with that raw proposition. It reconstructs the W917 assumption through `triangleCompletionOfRawFieldW918`, then projects back to W917 through `w917LeavesOfTriangleCompletionRawFieldW918`.

This is intentionally nonterminal. The W918 triangle-completion proposition remains an assumption in the closed-map branch route, and no original-four-task completion witness or final review packet is claimed.

After W918, the route still has 39 remaining explicit inputs. Deferred provider boundaries include source-image normalization, strict-preimage coherence consolidation, branch-selection or no-argument witnesses, endpoint facts, localized-right-adjoint data, and the final review packet.

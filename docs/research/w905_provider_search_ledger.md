# W905 Provider Search Ledger

- Baseline: W904 bundles W903 target data through `TargetSurjectiveCompactForTargetProviderW715`, while still taking `MappedExplicitCokernelClosedMapProviderW519` as a packaged mapped-cokernel closed-map input.
- Rejected branch-bundle candidate: rebundling `globalClosedMapComponents` and `mappedCokernelClosedMapProvider` through `MetrizableWppDiagramAndMappedCokernelClosednessDataProviderW725` would lose the closed-map branch guarantee unless the route also handled the closed-embedding branch. That is larger than a one-increment change.
- Chosen refinement: open the W519 mapped-cokernel closed-map provider to its raw field `∀ {X Y : MetrizableLCA} (f : X ⟶ Y), IsClosedMap (f : X → Y)`. This targets one concrete remaining proof obligation without changing the W899/W900/W901/W902/W903/W904 closed-map route.
- Result: W905 replaces W904's packaged W519 provider with the raw closed-map field and projects back into W904 for the stable ExactAt route. It remains nonterminal under the immutable original four-task contract.
- Remaining explicit inputs after W905: 28 for the closed-map mapped-cokernel-field target-data relation-data boundary-data source-triangulation endpoint-facts localized-right-adjoint route. The count is unchanged because a one-field provider was replaced by its one packaged field.
- Deferred candidates: W718 selected closed-map component fields, strict preimage object/map concrete fields, fixed-target uniqueness, source-image normalization, and the final branch selection/no-argument original-four-task witness.

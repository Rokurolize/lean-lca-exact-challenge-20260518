# W894 Provider Search Ledger

- Baseline: W893 splits the W716 WPP boundary-data bundle into `rightOpenBoundary` and `sourcePiZeroBoundary` while leaving the W717 closed-natural-transformation relation-data bundle intact.
- Chosen split: `ClosedNatTransOrdinaryRelationDataProviderW717`, because `WppOpExactAcyclicProjectionFields.lean` exposes its constructor fields directly as `relationPullbackProvider` and `targetRelationLiftsProvider`.
- Result: W894 replaces the bundled relation-data input with two explicit fields while preserving the W893 route and remaining nonterminal under the immutable original four-task contract.
- Remaining explicit inputs after W894: 36.
- Deferred candidates: W715 target compact-surjective data, W725 closedness branch, strict preimage object/map concrete fields, fixed-target uniqueness, source-image normalization, triangulated/source-side data, endpoint ExactAt facts, and bounded homotopy localized right-adjoint data.

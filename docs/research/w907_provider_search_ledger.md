# W907 Provider Search Ledger

- Baseline: W906 keeps the endpoint-homology-free closed-map route nonterminal and exposes W718 as the three W714 selected-difference closed-map component fields.
- Chosen refinement: open `MetrizableWppLimitBoundaryDataW716` into its two constructor fields: `MetrizableWppLimitRightOpenClosedQuotientCoverBoundary` and `MetrizableWppLimitSourceDifferenceCokernelPiZeroBoundary`.
- Reason: W716 is a plain two-field boundary-data structure. Splitting it gives the next proof search the actual right-open and source-pi-zero boundary obligations while preserving the W899-W906 closed-map branch route.
- Existing precedent: W893 already uses this field split in a different route lane; W907 applies the same boundary-data exposure to the current W906 branch.
- Result: W907 assembles W716 from the two boundary fields, projects into W906, and inherits the W906 stable ExactAt route. It remains nonterminal under the immutable original four-task contract.
- Remaining explicit inputs after W907: 31. The count rises because one bundled W716 provider is replaced by two actual proof fields; this is a concreteness refinement, not a product-success claim.
- Deferred candidates: W717 relation data fields, W715 target-surjective-compact fields, strict preimage object/map fields, fixed-target uniqueness, source-image normalization, branch selection/no-argument original-four-task witness, and the completion review packet.
